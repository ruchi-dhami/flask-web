name: devops app

on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]
jobs:
    build:
        runs-on: ubuntu
        steps:
            - uses: actions/checkout@v4
            - name: Set up Python
              uses: actions/setup-python@3
              with:
                python-version: 3.9